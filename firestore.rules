/**
 * @fileoverview Firestore Security Rules for the application.
 *
 * Core Philosophy:
 * This ruleset enforces a strict user-ownership model for user profiles. Accounts are publicly accessible, but modifications are not allowed.
 * Contacts and AccountProductNotes are owned by the account they are under. Products are public read-only.
 *
 * Data Structure:
 * - /users/{userId}: User profile information.
 * - /accounts/{accountId}: Sales accounts.
 * - /accounts/{accountId}/contacts/{contactId}: Contacts for an account.
 * - /accounts/{accountId}/products/{productId}: Product notes for an account.
 * - /products/{productId}: Product catalog.
 *
 * Key Security Decisions:
 * - Users can only read/write their own profile data.
 * - Accounts are publicly readable to allow listing all sales accounts.
 * - Account modifications (create, update, delete) are denied.
 * - Contacts are accessible only to authenticated users.
 * - AccountProductNotes are accessible only to authenticated users.
 * - Products are publicly readable but not writable.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Controls access to user profiles.
     * @path /users/{userId}
     * @allow (create) - If the user's auth UID matches the userId.
     * @allow (get, update, delete) - If the user's auth UID matches the userId.
     * @deny (create, update, delete) - If the user's auth UID does not match the userId.
     * @deny (list) - Listing all users is not permitted.
     * @principle Enforces document ownership for user profiles.
     */
    match /users/{userId} {
      function isOwner(userId) {
        return request.auth != null && request.auth.uid == userId;
      }

      allow get: if isOwner(userId);
      allow list: if false;

      allow create: if isOwner(userId) && request.resource.data.id == userId;
      allow update: if isOwner(userId) && resource.data.id == userId;
      allow delete: if isOwner(userId) && resource != null;
    }

    /**
     * @description Controls access to accounts.
     * @path /accounts/{accountId}
     * @allow (get, list) - Publicly readable to list accounts.
     * @deny (create, update, delete) - Account modifications are not permitted.
     * @principle Public read-only access for account listings.
     */
    match /accounts/{accountId} {
      allow get, list: if true;
      allow create, update, delete: if false;
    }

    /**
     * @description Controls access to contacts within an account.
     * @path /accounts/{accountId}/contacts/{contactId}
     * @allow (create) - Only authenticated users can create contacts.
     * @allow (get, list, update, delete) - Only authenticated users can access contacts.
     * @principle Restricts contact access to authenticated users.
     */
    match /accounts/{accountId}/contacts/{contactId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create: if isSignedIn();
      allow update: if isSignedIn() && resource != null;
      allow delete: if isSignedIn() && resource != null;
    }

    /**
     * @description Controls access to product notes within an account.
     * @path /accounts/{accountId}/products/{productId}
     * @allow (create) - Only authenticated users can create product notes.
     * @allow (get, list, update, delete) - Only authenticated users can access product notes.
     * @principle Restricts product note access to authenticated users.
     */
    match /accounts/{accountId}/products/{productId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create: if isSignedIn();
      allow update: if isSignedIn() && resource != null;
      allow delete: if isSignedIn() && resource != null;
    }

    /**
     * @description Controls access to products.
     * @path /products/{productId}
     * @allow (get, list) - Publicly readable for product listings.
     * @deny (create, update, delete) - Product modifications are not permitted.
     * @principle Public read-only access for products.
     */
    match /products/{productId} {
      allow get, list: if true;
      allow create, update, delete: if false;
    }
  }
}